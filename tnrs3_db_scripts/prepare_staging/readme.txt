Files in this directory complete loading and indexing of the staging table (`nameStaging`). Steps are not source specific.

Root script: prepare_staging.php, calls all others.
All parameters set in params.inc; see also global_params.inc.
