<?php

$sql_create_grin_taxonomy_tables="
DROP TABLE IF EXISTS `family`;
DROP TABLE IF EXISTS `genus`;

CREATE TABLE family (
	FAMNO INTEGER UNSIGNED NOT NULL,
	VALIDFAMNO INTEGER UNSIGNED DEFAULT NULL,
	FAMILY VARCHAR(250) DEFAULT NULL,
	FAMAUTHOR VARCHAR(250) DEFAULT NULL,
	ALTFAMILY VARCHAR(250) DEFAULT NULL,
	SUBFAMILY VARCHAR(250) DEFAULT NULL,
	TRIBE VARCHAR(250) DEFAULT NULL,
	SUBTRIBE VARCHAR(250) DEFAULT NULL,
	CMT VARCHAR(250) DEFAULT NULL,
	CREATED VARCHAR(50) DEFAULT NULL,
	MODIFIED VARCHAR(50) DEFAULT NULL,
	INDEX (FAMNO),
	INDEX (VALIDFAMNO),
	INDEX (FAMILY)
) TYPE=MYISAM DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci;
 
CREATE TABLE genus (
	GNO INTEGER UNSIGNED NOT NULL,
	VALIDGNO INTEGER UNSIGNED DEFAULT NULL,
	QUAL VARCHAR(50) DEFAULT NULL,
	GHYBRID VARCHAR(3) DEFAULT NULL,
	GENUS VARCHAR(250) DEFAULT NULL,
	GAUTHOR VARCHAR(250) DEFAULT NULL,
	SUBGENUS VARCHAR(250) DEFAULT NULL,
	SECTION VARCHAR(250) DEFAULT NULL,
	SERIES VARCHAR(250) DEFAULT NULL,
	SUBSERIES VARCHAR(250) DEFAULT NULL,
	FAMNO INTEGER UNSIGNED NOT NULL,
	FAMILY VARCHAR(250) DEFAULT NULL,
	OTHFAMILY VARCHAR(250) DEFAULT NULL,
	CNAME VARCHAR(250) DEFAULT NULL,
	CMT VARCHAR(250) DEFAULT NULL,
	CREATED VARCHAR(50) DEFAULT NULL,
	MODIFIED VARCHAR(50) DEFAULT NULL,
	INDEX (GNO),
	INDEX (VALIDGNO),
	INDEX (GENUS),
	INDEX (GAUTHOR),
	INDEX (FAMNO),
	INDEX (OTHFAMILY),
	INDEX (FAMILY)
) TYPE=MYISAM DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci;


";

?>
