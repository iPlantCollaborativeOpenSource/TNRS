#Modify /opt/tomcat/conf/catalina.policy Grant permission
#grant codeBase "file:${catalina.base}/webapps/" { 	
#	permission java.net.SocketPermission;
#};

#Modify httpd configuration file /etc/httpd/conf/httpd.conf. Add after modules declaration
### Mod jk configuration


JkWorkersFile conf/workers.properties
# Where to put jk logs
JkLogFile     logs/mod_jk.log
# Set the jk log level [debug/error/info]
JkLogLevel    debug
# Select the log format
JkLogStampFormat "[%a %b %d %H:%M:%S %Y] "
# JkOptions indicate to send SSL KEY SIZE,
JkOptions     +ForwardKeySize +ForwardURICompat -ForwardDirectories
# JkRequestLogFormat set the request format
JkRequestLogFormat     "%w %V %T"

#Copy the file _vhosts.conf into /etc/httpd/conf.d and edit
#Copy the file workers.properties into /etc/httpd/conf

