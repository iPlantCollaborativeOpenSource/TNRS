<?php
// Everything you need to set is here and in ../global_params.inc

// most parameters set here
include "../global_params.inc";

// confirmation message
$confmsg="\r\nLoad names for source='$sourceName' from staging table `$tbl` to database `$DB`? (Will replace existing records from this source!) (Y/N):";

?>
